'use strict';

describe('sprintMgmt.scrumBoard module', function() {

  beforeEach(module('sprintMgmt.scrumBoard'));

  describe('scrumBoard controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var scrumBoardCtrl = $controller('scrumBoardCtrl');
      expect(scrumBoardCtrl).toBeDefined();
    }));

  });
});
